// Intentionally empty. The docs site is fully static (Ox Content SSG emits
// every page in the client build's closeBundle hook), but Vite's client
// environment refuses to build without an entry module — this file satisfies
// it. See vite.docs.config.ts.
export {};
