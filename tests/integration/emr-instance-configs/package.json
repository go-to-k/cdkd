{
  "name": "cdkd-integ-emr-instance-configs",
  "version": "1.0.0",
  "private": true,
  "description": "cdkd integ: AWS::EMR::InstanceGroupConfig SDK provider (add a standalone TASK instance group to a single-node cluster, resize, destroy)",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "aws-cdk": "^2.1112.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.169.0",
    "constructs": "^10.0.0"
  },
  "type": "module"
}
