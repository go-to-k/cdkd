{
  "name": "cdkd-example-rollback-sqs-cooldown",
  "version": "1.0.0",
  "private": true,
  "description": "Exercises the cdkd rollback reverse-replacement through the SQS same-name deletion cooldown (#1206) and the failed-only journal retention cycle (#1208) against real AWS",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.169.0",
    "constructs": "^10.0.0"
  },
  "type": "module"
}
