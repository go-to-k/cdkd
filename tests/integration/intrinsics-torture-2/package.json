{
  "name": "cdkd-example-intrinsics-torture-2",
  "version": "1.0.0",
  "private": true,
  "description": "cdkd torture test #2 for harder CloudFormation intrinsic-function arg shapes (Fn::Select over list-intrinsic, FindInMap enhanced default, GetAtt Ref-attr, Sub ${!literal} escape, Base64 of intrinsic, nested If/Sub/Join, Cidr IPv6)",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.169.0",
    "constructs": "^10.0.0"
  },
  "type": "module"
}
