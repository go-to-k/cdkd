{
  "name": "cdkd-example-importvalue-chain",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end real-AWS test for a 3-stack Fn::ImportValue chain (A -> B -> C): A exports, B imports + re-exports a derived value, C imports B's export. Exercises chained strong-ref tracking, the exports index, and deploy/destroy ordering.",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.169.0",
    "constructs": "^10.0.0"
  },
  "type": "module"
}
