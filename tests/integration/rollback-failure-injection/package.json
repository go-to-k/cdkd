{
  "name": "cdkd-example-rollback-failure-injection",
  "version": "1.0.0",
  "private": true,
  "description": "Exercises the cdkd deploy-engine ROLLBACK path on a rich multi-resource stack (env-gated failure injection)",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.169.0",
    "constructs": "^10.0.0"
  },
  "type": "module"
}
