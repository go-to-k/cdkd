{
  "$comment": "Files under src/ that construct an @aws-sdk/client-* client WITHOUT spreading awsClientDefaults() first (issue 2388). PR 1 migrated aws-clients.ts, the bootstrap path and the state-write path — the files this list does NOT name — which is what makes the reporter's own repro pass. PR 2 emptied it: the list is now the assertion that no site is left, and re-adding an entry needs a rationale. This list may only SHRINK: scripts/check-aws-client-defaults.ts fails on a STALE entry (a file that is now clean) and on a DEAD one (a file with no client site), so finishing a file forces its entry out and the ratchet cannot quietly stop. Regenerate the candidate set with: node scripts/check-aws-client-defaults.ts --json",
  "files": []
}
