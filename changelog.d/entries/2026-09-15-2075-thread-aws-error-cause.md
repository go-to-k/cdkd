- **Six asset- and state-layer failures now thread the AWS error as `cause`.** The four `AssetError` arms (docker re-tag, ECR login, tag, push) and both `StateError` arms (`HeadBucket` reporting the state bucket missing, `PutObject` losing the optimistic-lock race) previously wrapped the caught value in an interpolated message and dropped it, so `isTransientServerError` / `isThrottlingError` / `isMarkedNonRetryable` classified on that message alone and `extractDeploymentEventError` lost the AWS error code and request id it persists. Issue #2075.
