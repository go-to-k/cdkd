- **`cdkd orphan` no longer saves a resource record whose `properties` map it could not read (issue [#3318](https://github.com/go-to-k/cdkd/issues/3318))** -- `src/cli/commands/orphan.ts`, `src/state/malformed-resources-bag.ts`, `docs/state-management.md`, `.claude/rules/state-malformed-properties-orphan.md`, `tests/unit/cli/orphan.test.ts`, `tests/unit/state/malformed-resources-bag.test.ts`. `rewriteResourceReferences` passes each surviving record's bag through `rewriteValue`, whose first line returns a non-object verbatim, and re-assigns it through a bare cast; `refuseMalformedState` above it answers a question about the record ROOT only, and `unreadableResourcePropertyBags` returns `[]` for a record whose root bag is unreadable -- so the per-ENTRY container was unguarded on a WRITE path. Measured through the real rewriter: `"abcdef"`, `5`, `null` and `[]` each came back byte for byte and an absent bag stayed absent -- nothing fabricated, but the run saved and reported success over a record the next `cdkd deploy` refuses ([#3191](https://github.com/go-to-k/cdkd/issues/3191)) and a `cdkd diff` in between previews as a replacement. A LIST bag is the one unreadable shape `rewriteValue` walks, so its rewrites landed in the audit table while the container stayed un-map-like. `refuseMalformedResourcePropertiesForOrphan` now refuses at the load, above the rewrite and the `--dry-run` return, with its own text (the deploy sibling's diff verdict and the `outputs` refusal's laundering sentence are both false here). It is SCOPED to the records the save would KEEP, so orphaning the damaged resource itself still works; the message leads with the two ways out needing no CDK app (hand repair, `cdkd state orphan <stack>`), since a construct path exists only while the app still declares the resource. No `--force` bypass. Residuals: [#3344](https://github.com/go-to-k/cdkd/issues/3344) (`state.orphans[]`), [#3345](https://github.com/go-to-k/cdkd/issues/3345) (`attributes`).
