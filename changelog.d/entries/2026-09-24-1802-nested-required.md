- **Deploy pre-flight now refuses a nested property block that omits a member CloudFormation requires (issue [#1802](https://github.com/go-to-k/cdkd/issues/1802))** -- `src/provisioning/nested-required.ts`, `src/provisioning/nested-required.generated.ts`, `src/provisioning/provider-registry.ts`, `scripts/gen-nested-required.ts`, `scripts/refresh-cfn-schemas.mjs`, `tests/fixtures/cfn-schemas/*.json`. cdkd used to forward such a block, and where the service API replaces the nested struct the omitted member was silently reset: an `AWS::ECS::Service` `DeploymentCircuitBreaker` without `Rollback` turned a live rollback off, while CloudFormation refuses the template. The check reads each type's schema `required` lists (a new `nestedRequired` fixture section, keyed by property path), refuses only a PRESENT block, skips anything behind an unresolved intrinsic, and runs only for the 78 types CloudFormation was measured to enforce these lists on. Types it was measured NOT to enforce them on (`AWS::Logs::LogGroup` accepts a tag with neither `Key` nor `Value`, among 11), the 3 EMR types, and types not measured are left unchecked. Aggregated into one error, with no override flag.
