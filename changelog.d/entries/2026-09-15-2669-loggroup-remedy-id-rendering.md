- **The `LogGroupClass` refusal's out-of-band disable command no longer pastes the state-borne log group name raw (issue [#2669](https://github.com/go-to-k/cdkd/issues/2669))** -- `src/provisioning/providers/logs-loggroup-provider.ts`, `src/provisioning/replacement-protection-advice.ts` (`renderDisableCommand` now exported), `tests/unit/provisioning/logs-loggroup-provider-class-guard.test.ts`, `tests/unit/provisioning/logs-loggroup-provider-partial-create-cleanup.test.ts`, `tests/integration/loggroup-class-guard/verify.sh`. The protected arm rendered `--log-group-identifier '${physicalId}'` by hand, so a `'` in the recorded id broke out of the quoting and a control byte forged terminal lines and `deployments/*.jsonl` lines. It now goes through the same sanitize / `shellQuote` / suppress the five #2610 sites use, keeping the log-group text around it: a clean id is rendered BARE (`--log-group-identifier /my/group`, the form the fixture greps), a quote is shell-quoted, and a non-ASCII or control-carrying id suppresses the whole command in favour of the `UNNAMEABLE_ID_CLAUSE`. The same-file create-cleanup `aws logs delete-log-group` command took the same rendering. **Behavior change:** message only -- the quotes around a clean id are gone. Residual: the other 40 pasteable `aws ...` commands in provider messages, [#3136](https://github.com/go-to-k/cdkd/issues/3136).
