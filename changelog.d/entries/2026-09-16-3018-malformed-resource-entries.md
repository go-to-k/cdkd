- **A state record cdkd cannot read is now refused where a command would WRITE it, and reported where it would not (issue #3018)** — `{"resources": {"R": null}}` passed `hasReadableResources`, which tests the BAG, and surfaced as a bare `TypeError`; so did a typeless object. `cdkd state refresh-observed` and `cdkd drift --accept` / `--revert` REFUSE (`STATE_RESOURCES_MALFORMED`), naming ids, before any lock or save; over several stacks refresh-observed pre-checks every region-scoped record, so one already malformed refuses the run before any write; a legacy record's refusal shows a `cdkd deploy` example only for an exact name with no `*`, `/` or leading `-`. Before, a LIST bag was refreshed and saved, and both drift writers persisted phantom `0`, `1` rows. Plain `cdkd drift` and `cdkd diff` REPAIR instead and warn, reporting each dropped row, or an unreadable map as one `(resources map)` row: `drift` as `notCompared` (new cause `unreadableRecord`), exiting 2 (1 if drift); `diff` in its preview and `--json` `unreadable`, counted by `--fail`. A dropped row the template declares previews as a CREATE. An unreadable rollback-orphan record is dropped before adoption and counted with them; a `null` one used to abort. `cdkd export`'s baseline report warns on every non-object bag, names unreadable entries outside its tally, sends the reader to repair, not a refusing `refresh-observed`, builds commands from the loaded stack and region, and withholds the writing `refresh-observed` command when rendering would alter its name or region, the name reads as an option, or the record has none; its identifiers are sanitized and capped by kind (stack 1152, id 255, region 128) — and every shared message, whichever command prints it, cuts a stack name at 1152. Its overlay preview cuts by code point. Changed: `src/state/malformed-resources-bag.ts`, `src/cli/commands/{state,drift,diff-recursive,export}.ts`, `src/utils/display-safe.ts`. Residual: #3202, #3232, #3307, #3335, #3339.
