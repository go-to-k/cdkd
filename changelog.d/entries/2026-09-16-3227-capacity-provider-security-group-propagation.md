- **A Lambda Managed Instances capacity provider created alongside its security group now RETRIES `One or more security group IDs are invalid` and the `ec2:DescribeSecurityGroups` rejection, instead of failing on the first one (issue [#3227](https://github.com/go-to-k/cdkd/issues/3227))** -- `src/deployment/retryable-errors.ts`. The create is issued within two seconds of the same stack's security group and operator role, and two more wordings of that race matched no `IAM_PROPAGATION_ERROR_MESSAGE_PATTERNS` entry, so neither was retried: `One or more security group IDs are invalid` and `doesn't have permission to perform ec2:DescribeSecurityGroups`, which the list's `not authorized to perform` does not cover. Three fresh deploys from empty state failed on the first wording. Both are now IAM-propagation patterns, so the create rides the dense 26-retry / 47.75s grid that issue [#3174](https://github.com/go-to-k/cdkd/issues/3174)'s operator-role entry already used. That budget is measured to cover the role and `ec2:` stages (a probe created 13s and 14s after the role); the security-group window is NOT measured, and the only timing evidence for it -- a re-deploy minutes later -- sits outside 47.75s. What broke was a SEQUENCE: one retry, then an unmatched wording ended the loop. Neither entry is scoped to Lambda or to a status code, so a genuinely wrong security group id, or a role genuinely missing that permission, spends the bounded retries before surfacing -- the trade this table already makes for `Invalid IAM Instance Profile`. The CAUSE is not established and the entries do not claim one: role permissions still propagating and the security group not yet visible to Lambda both fit, and the wording did not reproduce outside cdkd in six probe shapes. Live: `tests/integration/lambda-capacity-provider-default-name/` in ap-northeast-1 retried the `ec2:` wording on attempt 2 and created; the security-group wording did not occur in any run and is unit-covered only.
