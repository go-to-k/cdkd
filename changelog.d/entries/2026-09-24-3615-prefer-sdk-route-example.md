- **`--prefer-sdk-route` help and the `cdkd diff` routing example now name a property cdkd really does leave unwired (issue [#3615](https://github.com/go-to-k/cdkd/issues/3615))** -- `src/cli/options.ts`, `docs/cli-diff.md`. Both used `AWS::Lambda::Function:RuntimeManagementConfig`, which the SDK provider has written since [#1621](https://github.com/go-to-k/cdkd/pull/1621). So the help's only example was a no-op, the invalid-token error suggested the same token, and `docs/cli-diff.md` showed a `[via CC API: RuntimeManagementConfig]` annotation cdkd no longer prints. They now use `AWS::ApiGatewayV2::Api:Body`, an inline OpenAPI spec cdkd routes through Cloud Control by design, so the example does not go stale on the next back-fill. Doc comments in `diff-recursive.ts`, `provider-registry.ts` and `recreate-targets.ts` carried the same example and were updated too. No behavior change.
