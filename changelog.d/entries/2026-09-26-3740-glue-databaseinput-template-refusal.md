- **A template-path update now refuses a malformed Glue `DatabaseInput` block it used to deploy with a warning (issue [#3740](https://github.com/go-to-k/cdkd/issues/3740))** -- `src/provisioning/providers/glue-provider.ts`, `docs/provider-rules.md`, phase 2e of the `glue-update-hardening` fixture.
  - Refused on `cdkd deploy`, before any call to AWS: an `AWS::Glue::Database` update whose `DatabaseInput.TargetDatabase`, `FederatedDatabase` or `CreateTableDefaultPermissions` is malformed (a wrong shape, an unresolved intrinsic, an empty or partly unreadable block, or a permission entry cdkd cannot read), with the same message `create()` gives plus "Nothing was applied". It is refused even when the block did not change, because `UpdateDatabase` replaces `DatabaseInput` wholesale and sends every block on every update.
  - The rollback revert arms (`replayingState`) and `cdkd drift --revert` (`desiredFromAwsReadback`) keep the warning and still retain the previously applied block.
  - Unchanged: the `AWS::Glue::Table` `OpenTableFormatInput.IcebergInput` warning on update, since nothing is sent for it on update and no template edit makes it apply in place.
