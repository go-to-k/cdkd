- **`cdkd export` resolves the AppSync identifiers AWS re-declared in September 2026, blocks `AWS::AppSync::GraphQLApi` up front from a measured list, and `cdkd import` records a GraphQL API's `Arn` (issue [#3414](https://github.com/go-to-k/cdkd/issues/3414))** -- `src/cli/commands/export.ts`, `src/provisioning/providers/appsync-provider.ts`. AWS moved the API's `primaryIdentifier` from `ApiId` to `Arn` (issue #3327) and re-published `AWS::AppSync::ApiKey` with a read handler and the composite `[ApiId, ApiKeyId]`. Before: the API shipped its bare `apiId` as `Arn`; the key, no longer refused by the pre-flight, hit the `add an entry to COMPOSITE_ID_SPLITTERS` throw and aborted the export. After: the key has a splitter (`<apiId>|<apiKeyId>`, the key ARN, a bare pre-flip id with `ApiId` from properties; other `arn:` values refused), overlay narrowed to `ApiId` -- a standalone IMPORT changeset carrying one key reached `CREATE_COMPLETE` (us-east-1, 2026-09-18). The API resolves from the recorded `Arn` (tolerating a registry still reporting `ApiId` for a non-ARN physicalId), BUT the `export` integ run measured CloudFormation refusing the type for IMPORT despite its read handler, so a dated `CFN_IMPORT_REFUSED_DESPITE_REGISTRY` map now blocks it before the lock with the same remedies; `--skip-import-support-preflight` bypasses both checks. BEHAVIOUR CHANGE: an AppSync API used to fail at `CreateChangeSet` after locking the stack and now blocks pre-flight. `AppSyncProvider.import` records `ApiId` / `Arn` / `GraphQLUrl` for an adopted API (was `attributes: {}`). Live: the `appsync` fixture asserts an `export --dry-run` names exactly `GraphQLSchema` + `GraphQLApi` as blockers with the other identifiers resolved. No flag or schema change.
