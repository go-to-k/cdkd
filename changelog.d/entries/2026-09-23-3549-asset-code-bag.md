- **`resolveAssetCodeDirectory` takes every parameter in an options bag, so swapping the assembly-derived base with the attacker-chosen asset path no longer compiles** (issue [#3549](https://github.com/go-to-k/cdkd/issues/3549), PR #3556) -- No user-visible change. `src/local/lambda-resolver.ts`, `src/cli/commands/local-start-api.ts`. The function decides where a Lambda's `Metadata['aws:asset:path']` really lives for `cdkd local invoke` and `cdkd local start-api`, and positionals left FOUR adjacent `string` parameters with TWO transposable pairs. `(manifestDir, assetPath)` is the pair go-to-k/cdkd#3544 removed from the Docker twin, here with a worse sink: swapping them changes which directory is resolved and which is contained, for a value that is then BIND-MOUNTED into a running container. `(assetOutdir, logicalId)` made the containment bound `path.resolve('<logicalId>')` under the cwd, caught by tests rather than by the compiler. Required-ness answered only a DROP -- and that DROP was real, a call passing the outdir fourth and stopping there used to compile while binding it to `logicalId` and defaulting the bound to `manifestDir`, silently reinstating go-to-k/cdkd#3493's B1. Both call sites updated, behaviour identical, and the old positional spelling is now `error TS2554` (verified by compiling it). The interface is declared ABOVE its function: four of these doc blocks have detached from the declaration below them in this repo, each by an edit inserting between the two. New fence `asset-code-resolve-options-wiring.test.ts` pins the wiring, because `manifestDir` and `assetOutdir` are the SAME directory for a top-level stack -- a wrong or missing bound is invisible in every fixture that is not a Stage, and it FAILS CLOSED, which no refusal test can see.
