- **`resolveAssetCodeDirectory` takes every parameter in an options bag, so the positional swap between the assembly-derived base and the attacker-chosen asset path no longer compiles** (issue [#3549](https://github.com/go-to-k/cdkd/issues/3549), PR #3556) -- No user-visible change. `src/local/lambda-resolver.ts`, `src/cli/commands/local-start-api.ts`. The function decides where a Lambda's `Metadata['aws:asset:path']` really lives for `cdkd local invoke` and `cdkd local start-api`, and positionals left FOUR adjacent `string` parameters with TWO transposable pairs. `(manifestDir, assetPath)` is the pair go-to-k/cdkd#3544 removed from the Docker twin, here with a worse sink: swapping them changes which directory is resolved and which is contained, for a value that is then BIND-MOUNTED into a running container. `(assetOutdir, logicalId)` made the containment bound `path.resolve('<logicalId>')` under the cwd. Required-ness answered only a DROP -- and that DROP was real, a call passing the outdir fourth and stopping there used to compile while binding it to `logicalId` and defaulting the bound to `manifestDir`, silently reinstating go-to-k/cdkd#3493's B1. Both call sites updated, behaviour identical, old spelling now `error TS2554` (verified by compiling it). **Stated precisely, because the first draft of this entry overstated it:** a bag makes the swap UNORDERABLE, not inexpressible -- `{ manifestDir: assetOutdir, assetOutdir: manifestDir }` still compiles, and `local-asset-code-path-containment.test.ts` is what catches it, by driving both call sites against a STAGE manifest where the two directories differ. Measured: that spelling reds three cases there. The new `asset-code-resolve-options-shape.test.ts` pins only what no behavioural test sees -- the arity, and that the interface sits IMMEDIATELY above its function, four of these doc blocks having detached from the declaration below them by an edit inserting between the two.
