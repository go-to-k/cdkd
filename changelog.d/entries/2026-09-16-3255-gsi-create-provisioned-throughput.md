- **Every per-GSI `ProvisionedThroughput` now reaches AWS as a NUMBER, and a spelling CloudFormation rejects is dropped and named instead of forwarded (issue [#3255](https://github.com/go-to-k/cdkd/issues/3255) item 1, widened by its review)** -- `src/provisioning/providers/dynamodb-table-provider.ts`, `tests/unit/provisioning/dynamodb-table-provider-cfn-integer.test.ts`. FOUR per-index send sites forwarded the declared block verbatim: `create()`'s `CreateTable` mapper and all three `applyGsiUpdates` actions (the adopted-index repair, the `Create` and the same-name `Update`). So a CFn-legal `ReadCapacityUnits: '7'` went out as the STRING `"7"` in a `number` field and a `' 7 '` / `'0x9'` / `'1e1'` CloudFormation refuses the template for went out unchanged -- while the BillingMode-flip reader (`readCapacityNumber`) has read CloudFormation's measured DynamoDB Integer grammar (`/^[+-]?\d+$/`, no trim) since issue [#3147](https://github.com/go-to-k/cdkd/issues/3147). All four now read through it. Fixing only `create()` would have INVERTED the divergence rather than closing it -- one template succeeding on a fresh create and rejected when the same index was added later -- which is why the review pulled the other three in. A DECLARED member the grammar rejects is dropped and ANNOUNCED; an ABSENT member stays absent (the per-index reader deliberately has no `5` default); a non-object block is forwarded verbatim. The GUARDS around the update sites (`liveCapacityAlreadyMatches`, `skipZeroCapacityIndexUpdate`, the change detector) deliberately stay on the wider `toFiniteNumber`, so no deploy changes which calls it issues, only what they carry. The rejected spelling drops the MEMBER, not the block, so DynamoDB names the exact index position; reasoning for each decision is on `coerceIndexCapacityBlock` and `indexCapacityForSend`. Item 2 of the issue (`RecoveryPeriodInDays`) is untouched and it stays open for that.
