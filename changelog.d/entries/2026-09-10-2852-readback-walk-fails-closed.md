- **The readback position walk FAILS CLOSED, so a position it cannot certify no longer persists a decrypted secret (issues [#2852](https://github.com/go-to-k/cdkd/issues/2852), [#2846](https://github.com/go-to-k/cdkd/issues/2846), [#2869](https://github.com/go-to-k/cdkd/issues/2869))** -- `src/deployment/{secret-redaction,deploy-engine}.ts`, `src/cli/commands/{state,drift,scrub}.ts`, nine unit suites, one integ fixture, `docs/cli-{drift,state}.md`. **Bug:** the walk substituted the source expression only where it could CERTIFY and otherwise returned the raw bag -- the GHSA-p5qg-v9gv-hc7w disclosure (`state refresh-observed`, plain deploys). Reached by reshaped containers, normalised keys, reordered arrays, raw `Fn::Join` records ((#2846)), BINARY leaves. (#2012)'s rule-3 residual is retired: a refused indistinguishable position persists `***`, not the readback. **Fix:** such a position persists as `SECRET_MASK`, for a caller that DECLARES its bag a drift baseline (`STATE_SOURCED_BASELINE_RULES`) -- `cdkd state refresh-observed` and the deploy's own capture. Destination is not derivable: an `--accept` mask in `properties` blocks `cdkd export`. REPLAYED baselines keep the old answer ((#2886) closed: the journal passes the readback constant); `cdkd import`'s capture is one constant away ((#2885)). `--revert` now pairs masked array leaves BY IDENTITY, else per-LEAF corroboration -- index copying shipped a sibling's value ((#2884), masked-leaf half). Evidence comes from the PRE-token bag: the token pass's index copies cannot corroborate or identity-launder. The source is never written; a needle wins only on a WHOLE leaf. **(#2869) + (#2897) write half:** a non-plain object (`Date` / `Uint8Array`) corroborates NOTHING; the position/preserve walks return it BY IDENTITY, the untemplated merge takes the baseline -- never rebuilt as `{}`; rebuilds target null-prototype objects (own `__proto__` keys survive). **Still open:** an observed KEY the source does not carry.
