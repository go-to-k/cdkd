- **Every hook that sources the shared matcher now refuses (exit 2) and NAMES the constant when the library does not define one (issue [#2729](https://github.com/go-to-k/cdkd/issues/2729))** -- `.claude/hooks/lib/command-match.sh` (`gate_require_const` / `_soft` + `GATE_LIB_BASE_CONSTS`; its load-time self-interpolations now read `${BASE:-}` so a lagging library LOADS rather than aborting -- all 227 constant values byte-identical), 31 hook scripts, `.claude/rules/hooks.md`. `declare -F` guards FUNCTIONS and **cannot see a missing CONSTANT**, and per `.claude/rules/hooks.md` any exit that is not 2 propagates as a non-blocking error, i.e. a PASS. Measured against a materialised `origin/main` hook directory: **all 40 (hook, constant) reads fail** -- 25 at exit 1 (a `set -u` abort while sourcing), 5 at exit 0, and 10 more where the only thing naming the constant is bash's diagnostic rather than the gate -- i.e. 30 live fail-opens across 24 hooks, `branch-gate` (what stops a commit on `main`) and `ci-green-gate` (what stops a merge over red CI) among them. A second arm has no abort at all: with `set -u` set AFTER the source a missing BASE leaves every derived ERE non-empty but DEGRADED, so a check on the derived name passes while the gate stops matching `git -C <path> commit`. **`main-tree-edit-gate` takes the `Bash`-only carve-out** issue [#2717](https://github.com/go-to-k/cdkd/issues/2717) already gave its `declare -F` chain: the check was first written ahead of the tool-arm split and refused `Edit` and `Write` too, which was measured live when a merge conflict in the library locked all three tools out and the repair needed the maintainer's shell -- a safety mechanism must not remove the means of its own repair. Six paired cases fence it. NOT yet fenced as a class: residual [#2826](https://github.com/go-to-k/cdkd/issues/2826).
