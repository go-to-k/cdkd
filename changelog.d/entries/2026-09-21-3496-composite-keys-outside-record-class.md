- **Eight more composite keys are encoded rather than NUL-separated, so a planted name can no longer make one thing answer for another (issue [#3496](https://github.com/go-to-k/cdkd/issues/3496))** -- `src/state/record-keys.ts`, `src/analyzer/{lambda-vpc-deps,orphan-rewriter}.ts`, `src/deployment/intrinsic-function-resolver.ts`, `src/provisioning/providers/{idempotency-token,dynamodb-delete-budget}.ts`, `src/state/s3-replication-purge-gap.ts`, two suites plus one new one. These are the keys OUTSIDE the record class [#3323](https://github.com/go-to-k/cdkd/issues/3323) closed, found by that issue's whole-tree sweep. The consequential ones: `lambda-vpc-deps.ts` dropped a delete-dependency EDGE, so a Lambda could be deleted without waiting on the subnet or security group holding its ENI; `idempotency-token.ts`'s memo key is an IDENTITY, and a collision hands a second create the first's token -- the file documents that failure itself; the resolver's CFn-outputs cache could answer one stack's `Fn::GetStackOutput` with another stack's outputs, and its unrecognized-SSM-type warn set could drop the second warning about a parameter silently persisting as its expression; `s3-replication-purge-gap.ts`'s warn key joined FREE TEXT, so it was never injective. Every exemption reason that survived was rewritten as what is CHECKED rather than what a value is expected to be -- "CFn logical ids are alphanumeric" is refuted by cdkd's own comment that "nothing enforces that constraint HERE". `secret-redaction.ts`'s `maskedOutputKey` is deliberately left separated: the encoding lives in a module it would have to import and that file imports NOTHING by a recorded decision, while the collision adds no reach. Its two FALSE justifications are fixed instead. The `deploy-engine.ts` rows stay open on the issue -- two PRs held that file.
