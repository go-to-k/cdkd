- **A rollback replay keeps a `__proto__` property key, and `Fn::Sub` refuses a non-string template by name (issue [#2776](https://github.com/go-to-k/cdkd/issues/2776))** -- `src/deployment/rollback-executor.ts`, `src/deployment/intrinsic-function-resolver.ts`, `src/provisioning/providers/apigateway-provider.ts`, new `tests/unit/deployment/rollback-executor-proto-key.test.ts`, `tests/unit/deployment/intrinsic-prototype-chain-reads.test.ts`.
  - `resolveReplayProps` rebuilt each replayed bag onto a `{}` literal, so a property literally named `__proto__` (an own key after `JSON.parse`) ran the inherited setter and was silently absent from what `provider.update()` / `create()` received on a rollback. The walk now defines every key as an own property, the remedy #2767 applied to the resolver's object walk; the bag keeps its ordinary prototype, so providers see no other change.
  - A non-string `Fn::Sub` template (first element, or the scalar form) died at `template.matchAll is not a function`. It is now refused as `Fn::Sub: the first element must be a string, got <type>` / `Fn::Sub: the template must be a string, got <type>`, non-retryably, naming the type and never the value -- the terms the second element already had.
  - `resolveSub`'s variable lookups use `Object.hasOwn`. No behavior change: the maps have had a null prototype since #2739, so `${constructor}` already fell through to `Ref`; the rendered value (the kept placeholder) is now pinned. The `drift --revert` half of the issue shipped in #3124.
  - Swept from review: an `AWS::ApiGateway::Stage` update tested `key in variables` / `key in overrides` to decide removals, so dropping a stage variable (or canary override) named `constructor` / `toString` emitted no `remove` op and the variable stayed live. Both use `Object.hasOwn` now; the rest of that `in` population is a row on #3515.
