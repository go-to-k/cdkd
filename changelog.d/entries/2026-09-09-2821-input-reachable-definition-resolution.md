- **The nested-key critic judges a CFn definition against the SDK interface a REQUEST can carry, and the refreshed fixtures move three properties to Cloud Control (issue [#2821](https://github.com/go-to-k/cdkd/issues/2821))** -- `scripts/gen-nested-key-coverage.ts`, 19 refreshed `tests/fixtures/cfn-schemas/*.json`, `src/provisioning/property-coverage.generated.ts`, `docs/_generated/`. **Bug:** the shape pass resolved a CFn definition to the same-spelled SDK interface. Several services name the request shape `<X>Input` while `<X>` is the RESPONSE, so the match lands on a shape no request can carry and members existing only on the twin report `definition-member-missing` -- a CI-BLOCKING verdict whose remedy is an allow-list entry: a promise cdkd never sends a value it does send. AWS dropped the suffix from `AWS::Glue::Connection`'s definitions, turning four clean keys into blockers. **Fix:** `resolveDefinitionInterface` prefers the exact name when it is reachable from an operation input, else an `<X><suffix>` twin over `INPUT_ROOT_SUFFIXES`, else the exact name; the twin lookup is gated on the exact name EXISTING and on a half-members overlap, so neither an unmatched definition nor an unrelated shape is rebound. Reachability follows ref, array and map-value edges. Measured: 357 definitions, 6 not reachable, 5 with a twin; the old resolution reds those four keys and nothing else, the new one is clean with every other bucket identical. **User-visible from the refresh:** `AWS::Glue::Connection.Tags`, `AWS::CloudWatch::Alarm.WarmUpConfiguration` and `AWS::BedrockAgentCore::Runtime.CapacityProviderConfiguration` become known silent drops routed to Cloud Control; `AWS::RDS::DBCluster.AvailabilityZones` is no longer create-only (no replacement follows). `AWS::Glue::Connection` gains its FIRST `silentDrop`, so an existing SDK-provisioned connection setting `Tags` moves to Cloud Control and pins `provisionedBy: 'cc-api'` ONE-WAY -- it is not in `STICKY_CC_MIGRATION_EXEMPT`.
