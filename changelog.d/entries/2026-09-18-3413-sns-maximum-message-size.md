- **`AWS::SNS::Topic` now writes `MaximumMessageSize`, so the property AWS published on 2026-09-18 reaches the topic on the SDK route and the type's `'sdk-coverage'` exemption premise (an empty silentDrop map) holds again (issue [#3413](https://github.com/go-to-k/cdkd/issues/3413))** -- `src/provisioning/providers/sns-topic-provider.ts`, `src/provisioning/property-coverage.generated.ts` (regenerated), `tests/fixtures/cfn-schemas/_todo-backfill.json` (regenerated). Since the 2026-09-18 refresh the member was a silent drop, so a topic declaring it auto-routed through Cloud Control, and -- on the one type admitted to `STICKY_CC_MIGRATION_EXEMPT` on the premise that it has NO drops -- `cdkd diff` rendered `sticky` for a `cc-api` topic that `cdkd deploy --allow-unsupported-properties AWS::SNS::Topic:MaximumMessageSize` would flip to the SDK provider. Now: `create()` sends it inline on `CreateTopic`; `update()` sends a change through `SetTopicAttributes` and resets a template REMOVAL to `262144` (SNS's standard maximum) instead of the `''` the string members are cleared with, which SNS refuses for this integer (1024..1048576; all measured live, us-east-1, 2026-09-18); `readCurrentState` emits the member unconditionally as a number, folding an absent attribute to `262144` (a fresh topic reports nothing while one that has ever set it keeps reporting it, and both enforce the default), so every drift baseline this binary captures carries the key (a record observed by an older cdkd gains it on its next update or via `cdkd state refresh-observed`): a template removal compares equal to itself and a console-side change -- including an ADD after a removal -- is drift `--revert` can push back. The `cc-to-sdk-reroute` fixture carries the member through SDK create, the Cloud Control recreate, the SDK update on the flip, and a removal phase asserting the 262144 reset plus a clean `cdkd drift`. No flag or schema change.
