- **`cdkd drift --revert` no longer drops a baseline member whose key is literally `__proto__`, and the rebuilt node no longer refuses the preserve walks (issue [#2899](https://github.com/go-to-k/cdkd/issues/2899))** -- `src/cli/commands/drift.ts` (`resolveStateSecretExpressions`, `buildRevertNewProperties`, `setAtPath` / `getAtPath`, `collectNarrowedTopLevelKeys`), unit tests. The baseline walk rebuilt every object node onto a `{}` literal; `JSON.parse` on `state.json` yields an own `__proto__` key, and assigning it onto a literal set the node's prototype and dropped the key, so the member vanished from the revert payload and the polluted node failed `hasPlainPrototype` -- the token preserve walk returned it by identity and `mergeUntemplatedValue` stopped merging it. The walk rebuilds onto `Object.create(null)`, as the preserve walks already did. The same-class sweep of the file took the same remedy at every sibling: the revert overlay in `buildRevertNewProperties` and `--accept`'s `setAtPath` define the key as an own data property, `getAtPath` and the preserve walks read own keys only (a bare read answered `Object.prototype` for an absent segment), the narrowing delta rebuilds onto a null-prototype target, and every `key in bag` membership test (`mergeUntemplatedValue`, `collectMissingPaths`, `collectNarrowedTopLevelKeys`, the two plan reporters) is own-key — a property named `constructor` / `toString` (a legal env-var or tag name) read as present on a plain bag through the chain, so the untemplated merge dropped it from the revert payload. Residual, filed separately: the comparison chain's own normalisers (`src/analyzer/drift-normalize.ts` and siblings) still rebuild both sides onto `{}` literals, so a top-level `__proto__` member cannot be REPORTED as drift today -- a symmetric miss, never a wrong write.
