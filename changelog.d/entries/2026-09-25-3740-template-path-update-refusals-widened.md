- **A template-path update now refuses more malformed values it used to deploy with a warning (issue [#3740](https://github.com/go-to-k/cdkd/issues/3740))** -- `src/provisioning/providers/{iam-access-key,lambda-url,appsync,dynamodb-table,dynamodb-globaltable,s3-bucket}-provider.ts`, `docs/provider-rules.md`.
  - Refused on `cdkd deploy`, before any write to AWS: a malformed `AWS::IAM::AccessKey` `Status` or `AWS::Lambda::Url` `AuthType`; a changed, malformed `AWS::AppSync::GraphQLApi` `UserPoolConfig` / `OpenIDConnectConfig` / `LambdaAuthorizerConfig` / `AdditionalAuthenticationProviders` / `EnhancedMetricsConfig` / `EnvironmentVariables` block; a changed, malformed `BillingMode` on `AWS::DynamoDB::Table` and `AWS::DynamoDB::GlobalTable`, and `StreamSpecification` or a non-array `GlobalSecondaryIndexes` on the GlobalTable; and a malformed `AWS::S3::Bucket` lifecycle, notification, replication, object-lock, metrics, analytics, intelligent-tiering or inventory configuration that the update would Put.
  - The rollback revert arms (`replayingState`) and `cdkd drift --revert` (`desiredFromAwsReadback`) keep the warning: their properties are not editable from the template. A value unchanged from the recorded one is not refused where nothing is sent for it.
  - Kept as a warning on every path: `AWS::RDS::DBProxyTargetGroup` `TargetGroupName` and `AWS::Lambda::EventInvokeConfig` `Qualifier`, both create-only, so the only template fix is a replacement. The Glue `DatabaseInput` arm is not part of this change.
