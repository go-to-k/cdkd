- **A template-supplied identifier can no longer redraw your terminal from a cdkd message (issue [#3432](https://github.com/go-to-k/cdkd/issues/3432))** -- `src/deployment/intrinsic-function-resolver.ts`, `deploy-engine.ts`, `src/cli/commands/scrub.ts`, `src/utils/display-safe.ts`. **Bug:** go-to-k/cdkd#3426 closed this class at the masker and fixed the one render it measured; 19 siblings stayed behind a note whose premise ("CloudFormation requires a static string") answers the SECRET question, not the control-character one. It is false twice over: `resolveSub` re-enters `resolveRef` with unvalidated `${...}` text, and cdkd reads the template as JSON, so a KEY is only as constrained as the file. Measured: an id carrying `ESC[2K` + CR redrew five renders, two of them THROWS the user sees at any verbosity. **Fix:** per-site, not a blanket wrap. 18 render through `displayMasked` and their notes are deleted. The 19th is left alone -- reaching it means `resolvePseudoParameter`'s `switch` MATCHED, so the id is one of its own case labels. Review widened the population twice more (it had been defined by grepping ONE note spelling), reaching every remaining raw identifier render: the `Parameters` / `Conditions` name lines, `Fn::If`'s condition renders, the unsupported-intrinsic throw, the `AWS::NoValue` line, the two `display:` builders, and `DeployEngine.maskedRecordRemedyFor` -- which rendered a raw id into its `cdkd import ... --resource <id>=` command, now WITHHELD for an id outside `isPasteableIdent`, since sanitizing would name a different record `--force` accepts. Every site is pinned by its emitted BYTES, one case each. **Gate note:** all three `cdkd scrub` shape patterns now match an id whose only unmatched characters were invisible, moving such a unit `count` -> `warn`. Also DELETES the AST mask-coverage checker and its suite (1,760 lines) as high-maintenance tooling; go-to-k/cdkd#3426's entry names it as what holds this class, the AST containment fence does now.
