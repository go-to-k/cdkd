- **A failure-path state save no longer MASKS an `observedProperties` baseline `state.json` already holds intact (issue [#2906](https://github.com/go-to-k/cdkd/issues/2906))** -- `src/deployment/secret-redaction.ts`, `tests/unit/deployment/secret-redaction-uncertified-fail-closed.test.ts`, `secret-redaction-array-identity.test.ts`, `deploy-engine-secret-redaction.test.ts`. **Bug:** `scrubResourceRecord`'s rules derivation selected the fail-closed `STATE_SOURCED_BASELINE_RULES` for the observed bag whenever the per-resource secrets map was EMPTY. That is right for the fresh drained readback the refusal exists for, but `DeployEngine.redactStateForPersist` reaches the same arm on every failure-path and intermediate save for a resource whose resolve threw before recording anything -- and there the bag is the PRIOR generation's `observedProperties`, re-written UNCHANGED. A position the walk could not pair (an array AWS reordered, an identity key AWS normalised) became `SECRET_MASK`, producing permanent phantom drift no `cdkd drift` run clears until the next successful deploy overwrites the record -- the persist-path cousin of the journal poisoning closed as (#2886), and not a disclosure. **Fix:** the arm asks TWO questions now -- the map is empty AND the bag carries `markSameGenerationBag`'s mark, i.e. THIS RUN produced it. `drainObservedCaptures` marks every bag it installs and is the deploy path's only `observedProperties` writer, so the (#1900) / (#2852) / (#2885) refusal is unchanged for every bag it was written for; an unmarked bag is a previous generation's by elimination, and failing open there is safe for the reason (#2886) gives -- the value already sits in `state.json` verbatim. **Scope:** the derivation, so the `orphans` arm (#2948) added to the same function inherits it; `cdkd state refresh-observed` and `cdkd import`'s capture pass the constant explicitly and are untouched. **Not closed:** a REFUSED baseline refilled by a later writer -- (#2944).
