- **The rollback executor's replay log and `cdkd rollback`'s plan preview can no longer be forged or spoofed by a planted `rollback-journal.json` (issue [#3092](https://github.com/go-to-k/cdkd/issues/3092))** -- `src/utils/display-safe.ts` (new `displayIdent`), `src/deployment/rollback-executor.ts`, `src/cli/commands/rollback.ts`, `src/deployment/retry.ts`. Issue [#3064](https://github.com/go-to-k/cdkd/issues/3064) sanitized the preview and the parser; this is the layer that RUNS the plan, under `cdkd rollback` AND the deploy engine's automatic rollback. Fifty-three interpolations of a journal op's `logicalId` / `resourceType` / `changeType` into log lines and thrown messages were raw -- among them the pasted `--orphan <id>` remedy -- so an injected newline invented a line that read like the executor's own. Every identifier both layers render now goes through `displayIdent`: the ASCII allowlist with `<unrenderable>` for nothing-left, a 255-code-point cap naming how much it withheld, and a JSON-quoted rendering for a non-plain value -- the all-ASCII `X (AWS::RDS::DBInstance) -- already reverted` that survives any allowlist and planted the line's own wording inside a real row. Conditional, so every legitimate value renders as before. The five free-form SDK errors it renders take the denylist, `withRetry`'s lines sanitize their label, and the `--orphan` remedy is printed only for an id in CloudFormation's own logical-id charset (`~x` is plain but shell-expanded; otherwise it says why it is withheld). The new resource's `physicalId` from `state.json` (same bucket, same trust class) is sanitized on the same lines. **Deliberately still raw**, each pinned by a test: ids handed to a provider or used as state keys, the `msg` a collision classifier reads, and the `reason` strings recorded into `deployments/*.jsonl` (persisted; `cdkd events` sanitizes at render). A source-shape fence pins that a bare `${op.*}` exists only inside an event-reason statement or the gated remedy.
