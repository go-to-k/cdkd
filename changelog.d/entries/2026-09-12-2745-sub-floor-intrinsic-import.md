- **A 1-3 character secret embedded through an `Fn::Join` / `Fn::Sub` source, and one `cdkd import` resolves itself, now persist as their `{{resolve:...}}` token instead of plaintext (issue [#2745](https://github.com/go-to-k/cdkd/issues/2745), two of its three sites)** -- `src/deployment/secret-redaction.ts` (`positionByIntrinsicFrame` and its helpers), `src/cli/commands/import.ts`, unit tests, `tests/integration/secrets-dynamic-ref/` (`DB_PORT_SUB` / `DB_PORT_JOIN` / `PortJoin`, Guard 3d) and `tests/integration/import-secret-observed/` (`SubFloorParam`). The dominant CDK shape -- `'port:' + secret.secretValueFromJson('pin')`, an `Fn::Join` with the prefix fused into the token's opening part and the ARN `Ref` inside it -- had no positioning route: the skeleton arm answers only a WHOLE-token leaf and the literal span arm needs a source string, so the leaf fell to the value scan, which wrote a sibling's spelling above its needle floor and NOTHING below it. The new arm, run after the skeleton's refusal, renders the literal parts with the skeleton's own parser (a literal carrying the placeholder refuses), finds the one token, and writes the ONE candidate matching the token's pattern (the skeleton's caps and poisoning rule kept) over the skeleton's stores PLUS the pass-local pair table -- which alone names a collapsed loser, so an unpinned `ssm` whole-token join the skeleton refused is written back as itself -- when this pass resolved it to the middle, under the literal arm's bound or, below the floor, the engine's same-generation mark. `cdkd import` now marks the bag its own resolver produced (pair provenance). Residuals: a nonliteral intrinsic frame (a `Ref` outside the token) still falls to the scan; a public `ssm` leaf beside a secret sibling resolving to the same middle takes the sibling's token (a wrong reference, stated on the arm's docstring); the nested-stack twin stays with #2745; `maskSecretsInText` keeps the floor (#2453).
