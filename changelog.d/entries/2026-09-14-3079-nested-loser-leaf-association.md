- **Two nested-stack `Parameters` spelled through ONE literal frame around two DIFFERENT secrets resolving to the SAME 1-3 character value (`port:{{resolve:...A...}}` and `port:{{resolve:...B...}}`, both `port:q7`) now each persist their OWN frame in the child's `state.json` (issue [#3079](https://github.com/go-to-k/cdkd/issues/3079), residual (a) of [#2745](https://github.com/go-to-k/cdkd/issues/2745))** -- `src/deployment/secret-redaction.ts` (`recordNestedStackParameterExpressions`), unit tests, `tests/integration/nested-stack-secret/` (`PinTwinParam`). The parent's bag is keyed by plaintext, so the #2745 whole-value entry named one token and the losing parameter's child `{Ref}` leaf took the other token's frame -- a wrong reference `cdkd rollback` / `drift --revert` re-resolve after that sibling rotates. The recorder now also writes a per-NAME association (`name -> its own frame`) for every framed leaf passing the carry's conditions, survivor and loser alike, into the same table the whole-token walk fills; the child inherits it and reads it by name at the carry, its persist walk and `cdkd diff --recursive`. Condition (iii) gains a second arm: a loser's frame becomes the entry when every framed spelling of the value in the row carries that token, which closes the residual's other half (a loser under a DIFFERENT frame stayed plaintext). What remains: one child RESOURCE consuming both parameters holds one plaintext-keyed slot, and a leaf of it the association cannot reach by name (an `Fn::Sub` embedding the `Ref`) reads that slot's frame -- the #2320 class.
