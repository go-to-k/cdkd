- **An `Fn::ImportValue` export name from a template can no longer redraw your terminal (issue [#3426](https://github.com/go-to-k/cdkd/issues/3426))** -- `src/deployment/intrinsic-function-resolver.ts`, `scripts/check-resolver-mask-coverage.ts`. **Bug:** go-to-k/cdkd#3408 routed 97 render sites through `displayMasked` and fenced that spelling, and the class MOVED one indirection out -- to ten local bindings interpolated later, which a line-shaped scanner cannot see. Measured live on the DEFAULT deploy path at DEFAULT verbosity: an export name carrying `ESC[2K` + CR emitted a `warn` AND a `throw` that erase and rewrite the line (`raw CR 7` across both sinks in one `cdkd deploy`; 0 after). The dynamic-reference names, the attribute name, the `origin` builders and the parameter-value lines were the same shape. **Fix:** closed at the chokepoint, not at the sites. `maskSecretsForLog` is GONE -- the masking answer is `maskSecretsRaw`, reachable only from `maskThenStripThenMask`, itself reachable only from `displayMasked`, so no site can bind a bare masker. Also swept, each measured on the same repro: the region-scoped-clients debug line (no sanitizer at all), the invalid-region refusal (`stripControlChars` leaves U+2028), the `Ref <id> not found` warn (an `Fn::Sub` placeholder name is not validated) and every logical-id render in `src/analyzer/dag-builder.ts`, whose dangling-`DependsOn` warn was the last live one (`displayIdent` there). Messages still name what failed; an ordinary name renders byte-identically. **What keeps it closed:** the containment is asserted from the AST (one caller each), and `MASKERS` in the mask-coverage checker now means "masks AND sanitizes", so its walk -- which resolves an interpolated identifier, `let` included, to its declaration -- reports a bare-masker binding wherever it is rendered; no exclusion marker silences it. Cross-account refusal semantics are untouched: class, `markNonRetryable` marker and every message's WORDING are unchanged.
