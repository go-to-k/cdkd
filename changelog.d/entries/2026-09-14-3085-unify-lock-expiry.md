- **A lock's deadline renders the same everywhere it is shown, and a numeric-string `expiresAt` reads as unknown in the contention refusal as it already did in `cdkd state show` (issue [#3085](https://github.com/go-to-k/cdkd/issues/3085))** -- `src/state/lock-contention-message.ts` (new exported `formatLockExpiry`, replacing the private `formatRemaining`), `src/cli/commands/state.ts` and `src/state/lock-manager.ts` (their own copies from issue [#3083](https://github.com/go-to-k/cdkd/issues/3083) deleted in favour of the export), `docs/troubleshooting.md`, a new `tests/unit/state/lock-expiry-renderer-sync.test.ts` and a case in `tests/unit/state/lock-contention-message.test.ts`. The helper takes the RAW `expiresAt` and tests it with `Number.isFinite` -- the test `isLockExpired` makes -- so a numeric string, which the check treats as expired and the next acquire takes over, no longer prints `expires in ~Xm` in the refusal that advises against force-unlock. One user-visible wording change on the finite path: the contention refusal now prints seconds precision (`expires in 12m4s`, `expired 1m0s ago`) instead of `in ~12m` / `in under a minute` / `already expired`, matching the `LockError` refusal that advises the same decision and already did. The sync test refuses a fourth copy.
