- **Glue tag reads and tag updates target the right partition (issue [#1815](https://github.com/go-to-k/cdkd/issues/1815))** -- `src/provisioning/providers/glue-provider.ts`, `tests/unit/provisioning/glue-arn-partition.test.ts`. cdkd built every Glue ARN it hands to AWS as `arn:aws:glue:...`: the `AWS::Glue::Workflow` ARN behind its drift read's `GetTags`, and the shared builder behind the Job / Crawler / Trigger drift reads' `GetTags` and their `TagResource` / `UntagResource` tag updates. In `aws-cn` / `aws-us-gov` that ARN names no resource, so a tag update failed and a drift read reported the tags as empty. Both builders now derive the partition from the Glue client's region. Commercial-region output is unchanged. The same PR corrects in-code comments that still said `update()` takes no context or cannot tell a rollback replay from a template deploy (issue [#1999](https://github.com/go-to-k/cdkd/issues/1999)); comment-only, no behavior change.
