- **A divergent state record now also keeps `cdkd deploy`'s stateful-recreate probe conservative, and the destroy refusal withholds its target when the record's identity does not render exactly (issue [#3328](https://github.com/go-to-k/cdkd/issues/3328), review round 2)** -- `src/deployment/recreate-targets.ts`, `src/cli/commands/deploy.ts`, `src/state/malformed-resources-bag.ts`, `src/cli/commands/export.ts`, `tests/unit/deployment/recreate-targets.test.ts`, `tests/unit/state/state-key-region-authority.test.ts`, `tests/unit/cli/export.test.ts`. Two defects the sibling entries introduced. **(a)** `deploy.ts` feeds `state.region` into the `--recreate-via-*` probe's `expectedRegion`, whose mismatch arm refuses to read a `ResourceNotFoundException` as "this log group is empty". A divergent body USED to be that mismatch, so the arm fired and kept the data guard on; once the record's region is normalized to its key's the comparands agree by construction and a not-found clears the guard instead. `StatefulProbeClients.expectedRegionDiverged` restores the fail-closed outcome -- a BOOLEAN, not the value, because that arm renders its comparand in a default-verbosity warn. **(b)** The destroy refusal ended on `cdkd state orphan <stack> --stack-region <region>` while naming a stack whose rendering `displaySafe` may have TRIMMED -- so a record keyed `'prod-api '` opened byte-identically to a healthy `prod-api`, and an operator filling the template with the name above it orphans the intact record. It now computes the same `exact` test its sibling has carried since [#3332](https://github.com/go-to-k/cdkd/issues/3332) and, when it fails, names no target and sends the reader to `cdkd state list --long`. It also fails CLOSED on a bag it cannot count. `export.ts`'s `safeSegment` caps too -- the last uncapped rendering of a body-supplied region. `cdkd rollback` shares the inference and is [#3370](https://github.com/go-to-k/cdkd/issues/3370).
