- **A removed tag, attribute or map key named after an `Object.prototype` member (`constructor`, `toString`) is removed on update (issue [#3515](https://github.com/go-to-k/cdkd/issues/3515), its `in`-membership row)** -- `src/provisioning/json-patch-generator.ts`; the Glue (Job, Crawler, Trigger), Kinesis StreamConsumer, AppSync GraphqlApi, AgentCore Evaluator, Lambda MicrovmImage, S3 Tables (Table + TableBucket), S3 Vectors, CodeCommit, DLM, Cloud Map Service, API Gateway Method, API Gateway V2, S3 Bucket and CloudFront Distribution providers; `src/analyzer/diff-calculator.ts`, `src/analyzer/lambda-vpc-deps.ts`. One unit case per site (the two CloudFront tests share one).
  - A removal diff asked `key in newMap` of a plain object, which answers `true` for an inherited prototype member. So dropping such a key from the template sent no untag, remove or delete call, and it stayed live on AWS. Affected: a tag, a Cloud Control `remove` patch op, a Cloud Map service attribute, a V2 stage variable / integration request parameter / route request model, and a Method `requestParameters` entry. CloudFront skipped the removed top-level member instead of warning about it. A NEW key of that name was sent as `replace` instead of `add`, both on a Method map and in the Cloud Control patch. Every site uses `Object.hasOwn` now.
  - Three deep-equality checks compared an own `__proto__: {}` key against another key as EQUAL: the diff's nested-map compare (`NO_CHANGE` where it is an `UPDATE`), the Cloud Control patch (no `replace`), and the S3 lifecycle-rule fold (the unfolded `Filter` kept). The narrowing warning also names a dropped declared key of such a name now. The Lambda VPC delete-order pass no longer adds an edge to a `Ref` named `constructor` that names no resource.
