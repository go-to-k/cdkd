- **The distributed plugin skill is now covered by the documentation gate and checked against the real CLI** — `plugins/cdkd-skills/skills/cdkd/SKILL.md` tells an agent that never reads this repository which `cdkd` commands and flags to run, and nothing kept it in step with them. It was in the `check` gate only, so a behaviour change staled the `docs` marker through `src/**`, sent the author to `docs/` and `.claude/rules/`, and never named the plugin copy of the same claim. `plugins/**` joins the `docs` gate's scope, `/check-docs` names the file as a target to read, and a new test resolves every `cdkd <command> --flag` the page advertises against the real Commander tree, so a command or flag renamed or removed in `src/` fails instead of leaving the page telling an agent to run it. Issue #2673.
