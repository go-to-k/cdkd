- **`cdkd scrub` no longer reports a run CLEAN when a nameless dynamic reference aborted the resolve.** `{{resolve:ssm-secure}}` with no parameter name raises a bare `Error`, which scrub's best-effort catch swallowed to `debug` — and because the resolver aborts at the FIRST token, a real `{{resolve:secretsmanager:...}}` beside it in the same leaf was never fetched and recorded no needle, so a legacy plaintext already in `state.json` survived under `No plaintext secrets found`, exit 0. All four best-effort catches now re-raise it, pinned against the resolver's message by `tests/unit/cli/scrub-nameless-dynamic-ref.test.ts`. Issue #2692.
