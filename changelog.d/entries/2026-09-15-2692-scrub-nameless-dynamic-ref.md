- **`cdkd scrub` no longer reports a stack CLEAN when a nameless dynamic reference aborted the scan** — `{{resolve:ssm-secure}}` with no parameter name, and `{{resolve:secretsmanager}}` / `{{resolve:secretsmanager:}}` with no secret id, each raise a bare `Error` that fell through scrub's typed-refusal test into a `debug` line. Because resolution aborts at the FIRST token, a real secret reference beside it in the same leaf was never fetched and recorded no needle, so a legacy plaintext already sitting in `state.json` survived under `No plaintext secrets found`, exit 0. Both spellings now re-raise at all four best-effort catches. Issue #2692.
