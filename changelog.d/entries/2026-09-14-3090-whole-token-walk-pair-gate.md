- **A nested-stack `Parameters` entry spelled as a raw PUBLIC `ssm` token that a `cdkd import` record kept is no longer recorded as the child's reference on a rollback replay when its resolved value coincides with a secret the parent's bag holds (issue [#3090](https://github.com/go-to-k/cdkd/issues/3090), found in the [#3088](https://github.com/go-to-k/cdkd/pull/3088) review)** -- `src/deployment/secret-redaction.ts` (`recordNestedStackParameterExpressions`, the whole-token walk), unit tests. Under `STATE_DERIVED_RULES` the position pass certifies such a token with no pair evidence; refusal 1 passed on the secret's key and refusal 4 found nothing to disagree with, so the child's `{Ref}` leaf persisted the public reference and the next deploy reported one spurious UPDATE (the #1901 class, never a plaintext). A fifth refusal asks the pass's own pair table -- the same gate the sub-floor carry's per-name association already carried -- so a losing sibling (its pair recorded uncollapsed) and an unpinned `ssm` reference (a pass-local pair) still certify, while a token this pass never resolved falls to the value scan. Consequence for the `rules` argument: `trustAnyExpression` alone no longer changes what the recorder writes for a resolved reference (an unpinned `ssm` positions through the span arm's empty frame under either ruleset); the constants still decide `sourceIsSameGeneration`. Unit fixtures that hand the recorder a bare `Map` now record the pairs the resolver would.
