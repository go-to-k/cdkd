- **Pasteable `cdkd` commands move out of prose quotes and off bare placeholders, and a source fence reports three shapes that put them back (issues [#3436](https://github.com/go-to-k/cdkd/issues/3436), [#3307](https://github.com/go-to-k/cdkd/issues/3307))** -- new `scripts/check-pasteable-command-shapes.ts` + its test, `src/cli/commands/{deploy,destroy,drift,export,state}.ts`, `src/deployment/deploy-engine.ts`, `src/provisioning/providers/s3-tables-provider.ts`, `src/state/malformed-resources-bag.ts`. [#3499](https://github.com/go-to-k/cdkd/pull/3499) gave the repo one command BUILDER, and a grep for its NAME misses a site spelling its gate differently (measured on `gc.ts`). The fence keys on the SHAPE: a `cdkd` verb plus an interpolation inside a prose `'...'` span, a span assembled from two or more holes, or a bare `<placeholder>` with words after it. The tree reports zero NON-EXEMPT findings and the test asserts that, so a new site fails by name. One site is EXEMPT rather than fixed -- an `export.ts` refusal whose gate belongs in a follow-up PR -- and the exemption goes stale when that lands. No merge-base count is quoted: it depends on which recognizer version ran. What changes for users: a bare `<stack>` followed by a flag is two shell REDIRECTIONS, so pasting `cdkd events prune <stack> --all` where a file named `stack` exists exited 0, did nothing, and created `--all`. Those placeholders are quoted now. `cdkd drift`'s three remaining prose remedies and its untracked-properties plan note give the stack AND the region on a labelled line, gated by one predicate shared with the rest -- closing #3307's last three sites and its `--stack-region` requirement. `state.ts`'s three `Usage:` lines and `cdkd drift`'s revert summary spell arguments the way Commander does; `cdkd destroy --help` names `cdkd events prune '<stack>' --all`, which is that command's real arity with the placeholder quoted (a bare `<stacks...>` redirects too — measured).
