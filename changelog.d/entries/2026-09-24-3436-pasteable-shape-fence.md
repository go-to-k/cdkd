- **Pasteable `cdkd` commands move out of prose quotes and off bare placeholders, and a source fence reports three shapes that put them back (issues [#3436](https://github.com/go-to-k/cdkd/issues/3436), [#3307](https://github.com/go-to-k/cdkd/issues/3307))** -- new `scripts/check-pasteable-command-shapes.ts` + its test, `src/cli/commands/{deploy,destroy,drift,export,state}.ts`, `src/deployment/deploy-engine.ts`, `src/provisioning/providers/s3-tables-provider.ts`, `src/state/malformed-resources-bag.ts`. [#3499](https://github.com/go-to-k/cdkd/pull/3499) gave the repo one command BUILDER; a grep for its NAME misses a site spelling its gate differently, so the fence keys on the SHAPE: a `cdkd` verb plus an interpolation inside a prose `'...'` span, a span built from two or more holes, or a bare `<placeholder>` with words after. The tree reports zero NON-EXEMPT findings and the test asserts that, so a new site fails by name; one `export.ts` refusal is EXEMPT for a follow-up PR, and the exemption goes stale when that lands. For users: a bare `<stack>` before a flag is two shell REDIRECTIONS -- pasting `cdkd events prune <stack> --all` beside a file named `stack` created `--all` before the binary ran. Placeholders are quoted now. `cdkd drift`'s three remaining prose remedies and its untracked-properties plan note give the stack AND the region on a labelled line under one shared predicate -- closing #3307's last three sites and its `--stack-region` requirement. Its legacy region-less refusal names the stack on its `Stack:` line and in its `Migrate with:` command only for a plain identifier; otherwise no `Stack:` line, a quoted `'<stack>'` hole and a reason clause. `state.ts`'s three `Usage:` lines and `cdkd drift`'s revert summary print a QUOTED `'<stacks...>'` placeholder — Commander's `[stacks...]` is a bracket expression that expands to a one-character filename when pasted; `cdkd destroy --help` names `cdkd events prune '<stack>' --all`, that command's real arity.
