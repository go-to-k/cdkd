- **`cdkd import` can now clear a masked attributes bag.** A re-import whose second `GetResource` also yields no usable model carried the PREVIOUS masked bag forward, so the next deploy raised the identical refusal naming a remedy the user had just followed. The issue #1098 carry-over now skips a stored bag carrying the redaction mask, leaving `{}` — the shape a resource with no recorded attributes already has. Issue #2927.
