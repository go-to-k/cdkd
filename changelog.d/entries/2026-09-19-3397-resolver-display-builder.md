- **A stack / output / export name from a template can no longer redraw your terminal from ANY `cdkd deploy` resolver message (issue [#3397](https://github.com/go-to-k/cdkd/issues/3397) review)** -- `src/deployment/intrinsic-function-resolver.ts`; 97 render sites. `maskSecretsForLog` answers "does this text contain a recorded secret" and makes no claim about CONTROL CHARACTERS, so every message interpolating its result was masked but not stripped: a `StackName` carrying `ESC[2K` + CR emitted a line that erased and rewrote itself. It cannot simply strip -- its result is also a COMPARISON deciding whether a twin was registered -- so the two jobs are split: `displayMasked(value, context)` (mask, strip, mask, then `displaySafe`) is now the ONLY way a masked value reaches a message, and `displayLeaf` is its log-twin sibling for the `origin` builders. Both are in `MASKERS` in `scripts/check-resolver-mask-coverage.ts`, on the strength of delegating to the masker. Three review rounds each fixed the sites they found while the population was 86; three reviewers between them reached eight. `tests/unit/deployment/resolver-display-masked-population.test.ts` now fails on any `${this.maskSecretsForLog(...)}`, `${this.maskThenStripThenMask(...)}` or `${this.logTextOfLeaf(...)}`, so a new raw render IN THAT SHAPE cannot be added silently; it found two more sites nobody had reported on its first run. **Residual, stated rather than implied away**: the scanner is line-shaped, so ten LOCAL BINDINGS that take a raw masker result and interpolate it later are still uncovered -- one of them a live `Fn::ImportValue` exposure. go-to-k/cdkd#3426 carries the measurement and the reason it needs go-to-k/cdkd#3411's parser rather than another pattern. Cross-account refusal semantics are untouched -- class, `markNonRetryable` marker and every message's WORDING are unchanged.
