- **A secret a part records after the drain cap stopped waiting for it is no longer dropped, and every outputs reader that runs after it arrives redacts against it (issue [#2814](https://github.com/go-to-k/cdkd/issues/2814))** -- `src/deployment/deploy-engine.ts` (`ForwardingSecrets`, `absorbOutputsPassSecrets`), `src/deployment/intrinsic-function-resolver.ts` (`allSettledKeepingFirstRejection`'s `onAbandoned`), `src/deployment/drain-budget.ts`, `.claude/rules/layout-deployment.md` and `layout-deployment-secrets.md`. The #2563 drain's wait is capped, so a part it stops waiting for keeps running and records `plaintext -> expression` LATE. The `Export.Name` block resolved into a per-output map copied into the outputs pass map in a `finally`, so such a record was DROPPED and a later export name carrying that plaintext became a state key; it now resolves into a map that keeps its own entries (the exposure check's exact set) and writes each through to the pass map as it is recorded. The outputs bag was redacted when the pass ended and read unchanged afterwards; every `redactOutputs` now re-reads the pass map, and the final save, the exports index and the deploy summary (the no-change path's summary included) each redact the bag again when they read it, so a record landing during the observed-capture drain or the save's await reaches every one that runs after it. A drain the cap releases with parts still running now warns, once per budget, with the count. Not closed, and not closable without the wait the cap bounds: a reader that took its copy BEFORE the record arrived (a failure message already printed, a state save already sent). Cancelling the part instead would leave its plaintext missing for every reader.
