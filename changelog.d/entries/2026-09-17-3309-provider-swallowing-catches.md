- **Every graceful degradation under `src/provisioning/**` survives a caught value JavaScript cannot stringify, instead of becoming a hard failure (issue [#3309](https://github.com/go-to-k/cdkd/issues/3309) follow-up)** -- 61 files. A catch reading its caught value with `String(err)` -- bare, or behind the `err instanceof Error ? err.message : String(err)` ternary -- THROWS for a null-prototype object or a hostile `toString`. Computed on a path that can leave the handler WITHOUT throwing, that does not mean a worse message: it takes down whatever was degrading. Partial-create cleanups stopped running and left LIVE orphans while reporting a `TypeError` instead of the create failure that explains them (ACM, S3, five IAM types, API Gateway, ECS, SNS, EventBridge, Logs, SSM, DynamoDB GlobalTable); EC2's four delete retries died mid-loop, as did S3's not-empty retry, EventBridge's already-gone destroy and four pollers. Measured on the merge base, the population is **569 occurrences** -- **205 that can degrade** (61 files) and 364 that cannot. **204 of the 205 are swept** and the bucket is enforced at its one documented exemption (`cleanupFailedCreateRemnant`'s catch, whose bare arm is unreachable). The ternary form becomes `describeAwsFailure(err).detail`, byte-identical to it; the bare form becomes the new `safeStringify(err)` -- `String(err)` plus a guard, and **not** `.detail`, which drops the error's `name` and so shortens four persisted `outcome: 'partial'` reasons. Every site whose text has a downstream READER keeps it exactly and now names that reader at the call site -- retry / already-gone classifiers, ten that reach a THROWN, persisted message, and one that becomes a struct field read elsewhere. The remaining **363 occurrences** sit where the handler rethrows regardless, so the same defect substitutes a less useful error rather than breaking a degradation; deliberately not swept, tracked by issue [#3341](https://github.com/go-to-k/cdkd/issues/3341).
