- **Twelve state-reading sites no longer abort on a malformed `resources` bag** — `cdkd diff` (including the recursive nested walk), `import`, `orphan`, `scrub`, the prefix-migration check and the orphan rewriter all tolerate a `null` or absent bag rather than throwing before they can report on the very record that is broken. Issue #3018; the six sites in `destroy-runner.ts` / `deploy-engine.ts` are unchanged and remain open on it.
