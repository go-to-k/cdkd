- **No user-visible change** (issue [#3537](https://github.com/go-to-k/cdkd/issues/3537), PR #3542) -- `src/assets/{asset-manifest-loader,docker-build,file-asset-publisher}.ts`. `resolveFileAssetSourcePath` and `resolveDockerContextDirectory` take their trailing values as an options bag (`FileAssetResolveOptions` / `DockerContextResolveOptions`) instead of positionals. go-to-k/cdkd#3532 made `assetOutdir` required so that DROPPING it is a compile error; every remaining value is also a `string`, so a TRANSPOSITION still compiled -- `(…, assetOutdir, assetId)` on the Docker resolver bound the asset id to `sink` and printed `cdkd will cdkd-asset-<hash>`, and `(sink, assetOutdir)` on either bound a prose clause to the containment bound. The same PR deleted `FileAssetPublisher.publish`'s unused `profile` for that reason; this is the shape that was left. Six call sites updated, behaviour identical, and both old positional spellings are now type errors (verified by compiling them).
