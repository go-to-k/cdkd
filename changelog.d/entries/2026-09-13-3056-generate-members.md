- **A malformed MEMBER of `GenerateSecretString` is refused on create and SKIPPED on update, instead of minting a wrong secret or throwing (issue [#3056](https://github.com/go-to-k/cdkd/issues/3056))** -- `src/provisioning/providers/secretsmanager-secret-provider.ts`, `src/provisioning/config-shape.ts`, `tests/unit/provisioning/secretsmanager-secret-provider-update-value-source.test.ts`. The members of the block [#3048](https://github.com/go-to-k/cdkd/issues/3048) guarded were still read by cast. **Bug:** `PasswordLength: 'abc'` (truthy) reached `new Uint8Array('abc')` and minted an EMPTY password; a truthy `'false'` EXCLUDED punctuation; an unparseable `SecretStringTemplate` sent the bare password RAW where a JSON document was declared -- as the new `AWSCURRENT`, silently; and `ExcludeCharacters: null` THREW from `update()`, the #1544 hazard one level below the container. **Fix:** every member is read through the shared `config-shape.ts` predicate for its type -- the new `configIntegerRefusal` / `coerceCfnInteger` pair for `PasswordLength` (a CFn `"32"` coerces; `0` / `3.5` / `'abc'` / `'1e2'` / `null` refuse), the boolean pair for the `Exclude*` flags, `configStringRefusal` for the three strings -- plus a PAIR rule measured on CloudFormation (`SecretStringTemplate and GenerateStringKey must both be set or removed`): together or not at all, the template a JSON object, the key not `__proto__`. `PasswordLength` is capped at 4096. The retention of the previous block runs the SAME member predicate, so a stale record with a malformed member is dropped. On the create path a refusal throws (template-borne); on the update path `generateSecretString` hands the message to the same `onUnusable` skip the container takes, so the value is SKIPPED, announced once, and the previously-applied block is retained via `effectiveProperties`. `IncludeSpace` / `RequireEachIncludedType` (declared, never read) are a fidelity gap: [#3068](https://github.com/go-to-k/cdkd/issues/3068).
