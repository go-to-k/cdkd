- **`Fn::GetAtt [<SecurityGroup>, VpcId]` on a security group declared WITHOUT `VpcId` (a hand-written L1 landing in the account's default VPC) now resolves to the default VPC's id, as CloudFormation answers, instead of `''` from the record or the literal `'undefined'` inside an `Fn::Join` / `Fn::Sub` (issue [#3097](https://github.com/go-to-k/cdkd/issues/3097))** -- `src/provisioning/providers/ec2-provider.ts` (`readSecurityGroupVpcId`), `src/deployment/intrinsic-function-resolver.ts` (the `AWS::EC2::SecurityGroup` `VpcId` arm, `isImpossibleEmptyStoredAttribute`), a default-VPC group and its Output in the `ec2-instance` integ. The provider copied the TEMPLATE's own `VpcId` with `?? ''` (the last two sites #3077 exempted), so a template with no `VpcId` recorded `''`; the resolver arm behind it answered `undefined`. Both halves now read the group back: the provider records `VpcId` from a best-effort `DescribeSecurityGroups` after create and update (a not-found answer, EC2's read-after-write lag, is retried up to 5 attempts a second apart), omitting the key (never `''`) when the read fails or reports none, the error class at debug; the resolver re-reads it live on an absent key -- `GroupIds`, cached per deploy like the #3096 arms -- and REFUSES (`refuseUnservedAttribute`, unmarked, retryable) on an empty or failed read rather than answering the group id, after a marked `sg-<hex>` shape guard on the persisted physical id. A LEGACY record holding `VpcId: ''` (a pre-#3097 binary, group declared without `VpcId`) is read as absent for this one attribute (no security group has no VPC), so the live arm answers; the record keeps `''` until the next `update()`. `GroupId` is unchanged. The attribute-map fence's allow-list is now empty; the handled-property-wiring matrix records `VpcId` seeded by the create seams only (the update-path read was the attribute copy).
