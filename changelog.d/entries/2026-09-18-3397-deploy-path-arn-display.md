- **A user-supplied IAM role ARN can no longer redraw your terminal from a DEPLOY-path message either (issue [#3397](https://github.com/go-to-k/cdkd/issues/3397), completing the class go-to-k/cdkd#3390 closed for `cdkd local`)** -- `src/deployment/intrinsic-function-resolver.ts`, `src/utils/role-arn.ts`, `src/cli/options.ts`, `src/cli/commands/drift.ts`; 14 sites plus `src/cli/commands/rollback.ts`, each through `displayIdent` with `ROLE_ARN_MAX_CODE_POINTS`, so an ordinary ARN renders byte-identically (an IAM PATH admits `( ) ! # $ % & * [ ]`, which now render JSON-quoted -- recorded at the site). The issue named five sites, derived by grep. Widening the go-to-k/cdkd#3390 display FENCE (`tests/unit/cli/local-profile-display-population.test.ts`, whose `inArnScope` carries the measurements) to the four USER-SUPPLIED-ARN surfaces found ten: two `--assume-role` argparse refusals in `src/cli/options.ts` -- argv, whose `IAM_ROLE_ARN_REGEX` is START-anchored, so even an ACCEPTED value is unconstrained past `role/` -- and five in `src/utils/role-arn.ts` itself, the file the issue body cites as having adopted this rule in issue [#2170](https://github.com/go-to-k/cdkd/issues/2170). It had, in ONE of its two functions; `assumeRoleForCrossAccountStateRead` rendered `roleArn` raw at all five of its sites -- `AssumeRole into ${roleArn} failed: ${message}`, where STS echoes an unparseable RoleArn back verbatim, so the value returned past the guard through the error text. That neighbour now takes `displaySafe`, as does `drift.ts`'s IAM error text. **The cross-account refusal semantics are untouched** -- `CrossAccountSecretRefusalError`, its `markNonRetryable` marker and the message wording are unchanged, so `cdkd scrub`'s `instanceof` branch and every retry classifier behave exactly as before. NOT widened tree-wide: that reports 153 sites under `src/provisioning` / `src/synthesis`, out of scope by REACHABILITY (go-to-k/cdkd#3405).
