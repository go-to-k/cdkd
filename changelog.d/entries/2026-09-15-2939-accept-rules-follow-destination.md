- **`cdkd drift --accept` and the `--revert` narrowing write mask an UNCERTIFIABLE position instead of persisting a stored plaintext when the bag lands in `observedProperties` (issue [#2939](https://github.com/go-to-k/cdkd/issues/2939))** -- `src/cli/commands/drift.ts` (`runAccept`'s re-redaction, `runRevert`'s narrowing write), `.claude/rules/layout-drift.md`, `.claude/rules/layout-deployment-secrets.md`, unit tests. Both writers passed the non-failing `STATE_SOURCED_READBACK_RULES` unconditionally although each site already knows its destination: `--accept` computes `hasObserved` two lines away, and the narrowing write persists a VALUE only against an observed baseline. Each now selects `STATE_SOURCED_BASELINE_RULES` (fail-closed, the constant `cdkd import` and `cdkd state refresh-observed` declare) when the bag is a drift baseline and keeps the readback constant when it lands in `properties`, where a mask blocks `cdkd export` and the rollback replay. Net effect: a pre-#2852 record carrying a rotated-away plaintext at a position the walk cannot pair is cleaned to `SECRET_MASK` by the `--accept` it was invoked for, and a provider echo the walk cannot pair is masked in the baseline rather than persisted decrypted; a masked position reports as drift, `--accept` refuses it and `--revert` preserves the live value there until a deploy rewrites the baseline. A clean record is never made worse.
