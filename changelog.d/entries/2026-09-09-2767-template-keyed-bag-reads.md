- **Every read of a resolver or template-parser bag by a template-controlled name is own-key now, and a `__proto__` key stops being silently dropped (issue [#2767](https://github.com/go-to-k/cdkd/issues/2767))** -- `src/deployment/intrinsic-function-resolver.ts`, `src/analyzer/template-parser.ts`, two new test files. Fourteen sites: a name that is an `Object.prototype` member answered where no entry existed, mostly as a WRONG RESULT rather than a refusal. `Ref` / `Fn::GetAtt` read `resources['constructor']` as the `Object` FUNCTION and shipped `undefined` into a live property; `Fn::If` took the TRUE branch where an unknown condition assumes false; `Fn::FindInMap`, whose keys are free-form text, returned the function as the value; and a `SecretString:constructor` reference rendered the function's SOURCE as the resolved secret. Three WRITES lost a `__proto__` key to the inherited setter, the worst being `filterResourcesByCondition` -- a declared resource vanished from the effective template, so the diff read "in state, absent from desired" and issued a DELETE for it. Reads use `Object.hasOwn` (membership, NOT a value test, so a DECLARED parameter holding `undefined` still returns it, per [#2285](https://github.com/go-to-k/cdkd/issues/2285)); the value-bearing writes SHADOW the key with `defineProperty`, since `Object.create(null)` would make `String()` on a resolved object throw. Per-site mechanism and the probe results are in the two test files' doc comments. **The method is the durable part**: four review rounds each surfaced sites the last had missed, so the third replaced grep-and-guess with an ENUMERATION of every `in` test and identifier-keyed index in both files -- and the fourth showed that enumeration had been complete while the TRIAGE missed three, each dismissed without reading its callers. Residual [#2776](https://github.com/go-to-k/cdkd/issues/2776): the downstream walks in `rollback-executor.ts` / `drift.ts`, and `resolveSub`'s own map.
