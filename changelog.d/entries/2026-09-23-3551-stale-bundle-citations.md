- **A Lambda layer ARN in ANY partition downloads under `cdkd local invoke`, and the docs said otherwise for four weeks after the upstream fix** (issue [#3551](https://github.com/go-to-k/cdkd/issues/3551), PR #3557) -- No code change. `docs/local-invoke.md`, `src/local/lambda-resolver.ts`, `src/cli/commands/local-start-{alb,cloudfront}.ts`, three tests. `docs/local-invoke.md` told users an ARN in `aws-cn`, `aws-us-gov`, `aws-eusc` or the four ISO partitions "passes cdkd's parse and then fails at the AWS call, because the download rebuilds the ARN with a hardcoded `aws` partition". Measured on the installed engine: `fetchLayerContentUrl` strips only the `:<version>` suffix from the caller's ARN and hands the remainder to `GetLayerVersionCommand` as `LayerName`, so the partition survives. go-to-k/cdk-local#575 closed 2026-08-27 and shipped in cdk-local 0.147.13 three minutes later -- and it reached USERS immediately, because cdkd declares `cdk-local` as a plain runtime dependency with no `bundledDependencies`, so a consumer resolves the RANGE, and the range then in effect (`^0.147.7`) already admitted it. Only this repo's pinned tree lagged, to the 0.148.4 bump on 2026-09-12. **Why it went unnoticed for four weeks:** SIX sites cited `cdk-local@0.147.7` and the bundle chunk `dist/local-studio-BBtUAVNy.js`, a content-hashed name regenerated every release, so each claim read as verified while being uncheckable. The SEVENTH -- the user-facing page, the only one that mattered to a reader -- cited nothing at all, which is why grepping for the stale citation could not find it and searching for the stale CLAIM was required. The six were rotted line numbers behind claims that still hold, each re-measured one at a time. Every site now names the package VERSION and the SYMBOL NAME, and carries the `grep` that re-checks it; the symbols are not `export`ed, they are findable because the bundle is unminified.
