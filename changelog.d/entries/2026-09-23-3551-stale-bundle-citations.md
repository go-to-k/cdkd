- **Lambda layers in the five ISO partitions and `aws-eusc` do download under `cdkd local invoke`, and a comment claiming otherwise outlived the upstream fix by a month** (issue [#3551](https://github.com/go-to-k/cdkd/issues/3551), PR #3557) -- No code change. `src/local/lambda-resolver.ts`, `src/cli/commands/local-start-{alb,cloudfront}.ts`, three tests. Six sites cited `cdk-local@0.147.7` and the bundle chunk `dist/local-studio-BBtUAVNy.js`, neither of which exists any more; the installed chunk is `local-studio-D8cHeCcp.js` at 0.149.1. Five were rotted LINE NUMBERS behind claims that still hold -- re-measured one by one, `bootOneTarget` / `rollOneTarget` still hand `createLocalStateProvider` the full options bag and `resolveDeployedS3Origins` / `attachKvsModules` still gate on `isCfnFlagPresent`. **The sixth was a false claim about behaviour.** `lambda-resolver.ts` said cdk-local rebuilds a layer ARN with a hardcoded `aws`, so a layer outside the commercial partition "still fails at `lambda:GetLayerVersion`". Measured on 0.149.1: `fetchLayerContentUrl` strips only the `:<version>` suffix from the caller's ARN and hands the remainder to `GetLayerVersionCommand` as `LayerName`, preserving whatever partition the ARN names -- go-to-k/cdk-local#575 closed on 2026-08-27 and nothing here noticed, because the citation named a content-hashed file nobody could check. The dependent sentence below it, which justified an early refusal by pointing at that rebuild, now stands on its own terms. Every site now cites the package VERSION and the exported SYMBOL, both of which survive a rebuild, and carries the `grep` that re-checks it; the chunk filename and the line numbers are gone, being unverifiable inside a minified bundle either way.
