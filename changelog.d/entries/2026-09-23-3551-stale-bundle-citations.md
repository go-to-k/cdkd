- **A Lambda layer ARN in ANY partition downloads under `cdkd local invoke`, and the docs said otherwise for a month after the upstream fix** (issue [#3551](https://github.com/go-to-k/cdkd/issues/3551), PR #3557) -- No code change. `docs/local-invoke.md`, `src/local/lambda-resolver.ts`, `src/cli/commands/local-start-{alb,cloudfront}.ts`, three tests. `docs/local-invoke.md` told users that an ARN in `aws-cn`, `aws-us-gov`, `aws-eusc` or the four ISO partitions "passes cdkd's parse and then fails at the AWS call, because the download rebuilds the ARN with a hardcoded `aws` partition". Measured on the installed cdk-local 0.149.1: `fetchLayerContentUrl` strips only the `:<version>` suffix from the ARN the caller supplied and hands the remainder to `GetLayerVersionCommand` as `LayerName`, so the partition survives -- go-to-k/cdk-local#575 closed on 2026-08-27 and nothing here noticed. **Why it went unnoticed:** seven sites cited `cdk-local@0.147.7` and the bundle chunk `dist/local-studio-BBtUAVNy.js`, a content-hashed name that is regenerated every release, so the claim read as verified while being uncheckable. The other six were rotted LINE NUMBERS behind claims that still hold, each re-measured one at a time (`bootOneTarget` / `rollOneTarget` still hand `createLocalStateProvider` the full options bag; `resolveDeployedS3Origins` / `attachKvsModules` still gate on `isCfnFlagPresent`, which `start-api` does not, which is why the same flags work there). Every site now names the package VERSION and the SYMBOL NAME -- both survive a rebuild, unlike the chunk filename and the offsets -- and carries the `grep` that re-checks it. The symbols are not `export`ed; they are findable because the bundle is unminified.
