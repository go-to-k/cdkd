- **An embedded 1-3 character secret now persists as its `{{resolve:...}}` token instead of plaintext, on the bags the deploy pass produced (issue [#2516](https://github.com/go-to-k/cdkd/issues/2516); residuals [#2745](https://github.com/go-to-k/cdkd/issues/2745))** -- `src/deployment/secret-redaction.ts` (`markSameGenerationBag`, the relaxed bound in `positionByEmbeddedSpan`), `src/deployment/deploy-engine.ts`, three unit test files, and `tests/integration/secrets-dynamic-ref/` (`DB_PORT_LITERAL` / `SECRET_PIN_STAGED` / `PortLiteral`, with Guard 3c and the Phase 1e / 1g checks). The value scan's SUBSTRING arm drops every plaintext under four characters (its exact-match arm has no floor), so a literal leaf embedding one persisted `port:q7` in `properties`, the readback and a `CfnOutput`; #2485's span arm bound itself to the scan's answer, which below the floor proves nothing on an old bag. The arm now accepts that silence ONLY on a bag whose generation the ENGINE proved: a `WeakSet` mark on the record's `properties`, the `drainObservedCaptures` readback and the `outputs` bag, plus copies at the re-check and the journal. Composes with #2788: the record's bag is the resolved one MINUS the keys the SDK route drops, so the mark is taken AFTER that narrowing -- before it, the narrowed copy is unmarked and the leaf plaintext again. Read for the object handed to `redactSecretsForState`; an old record, a scrub / import / drift walk and a sub-bag alone stay unmarked. Per-site conditions are on the function's docstring. Residuals: `effectiveProperties`; a coinciding readback; an interference refusal leaving the sub-floor middle (silent when the interferer is itself sub-floor -- the OTHER secret under-redacted, never fabricated -- unless it matches the WHOLE leaf); `maskSecretsInText`, sharing the floor (#2453); `cdkd scrub`, walking a stored bag no deploy marked; the `Fn::Join` / `Fn::Sub` source shape -- with the nested-stack and `cdkd import` twins, #2745.
