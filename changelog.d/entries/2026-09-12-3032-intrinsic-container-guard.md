- **An unresolved intrinsic is no longer accepted as a CFn config CONTAINER, so the block stops silently emptying (issue [#3032](https://github.com/go-to-k/cdkd/issues/3032))** -- `src/provisioning/config-shape.ts`, plus its unit suites. **Bug:** the container gates tested `isPlainObject`, but `{ "Ref": "X" }` **is** a plain object -- so it passed, every member probe indexed to `undefined`, and the block read as EMPTY: the silent DROP this family exists to refuse (#1581). **All FOUR gates had it**, not just `requireConfigObject`: the siblings are safe only in their FIELD halves, and review measured `readConfigString` returning the FALLBACK for an `Fn::If` `VersioningConfiguration` -- versioning OFF on a live bucket, this module's own #1471 headline through the new shape. **Widest site:** the replication `Filter`, whose fall-through emits `Filter: {}` -- valid CFn for "replicate EVERY object" -- so an intrinsic widened replication to the whole bucket; a lost lifecycle `Filter` expired it bucket-wide. **Reach is dominated by the STATE-borne replay paths** (the rollback executor's reverse-replacement create and revert arms, `cdkd drift --revert`). A fresh template is mostly but not wholly excluded: the resolver throws on an unresolvable `Ref` / `Fn::GetAtt` and on an unknown `Fn::*`, but `Fn::Transform` has no dispatch arm and arrives verbatim -- refusing it is right anyway, an unexpanded transform AS a container meaning the macro never ran. **Fix:** a single-key `Ref` / `Fn::*` object is unusable, taking each site's EXISTING arm: a refusal where the site refuses (UPDATE sites included -- several deliberately do, per #1493/#1471, their fallback blanking a live value), warn-and-skip where it passes `onUnusable`. No call site changed. The message names the cause, not `got an object`. The predicate is deliberately NOT shared with the other near-copies; its doc comment says why, and a test pins its `keys.length === 1` boundary against the broader `keys.every` spelling.
