- **`VectorIndexes` on `AWS::DynamoDB::Table` / `AWS::DynamoDB::GlobalTable` is a known silent drop routed to Cloud Control, instead of an unrecognized property dropped with a warn (PR [#3167](https://github.com/go-to-k/cdkd/pull/3167))** -- `tests/fixtures/cfn-schemas/` (`AWS-DynamoDB-Table.json`, `AWS-DynamoDB-GlobalTable.json`, `_todo-backfill.json`), `src/provisioning/property-coverage.generated.ts` (regenerated). The daily schema refresh picked the property up from AWS's public CloudFormation bundle, along with its nested `VectorIndex` / `VectorAttribute` / `SearchSchemaElement` definitions. Before this, `VectorIndexes` post-dated the committed fixture snapshot, so pre-flight read it as UNRECOGNIZED -- for a resource on the SDK route with no other actionable drop and no `provisionedBy: 'cc-api'` record, `reportUnrecognizedProperties` warned and the SDK provider dropped it while the deploy reported success (the issue [#614](https://github.com/go-to-k/cdkd/issues/614) failure class that machinery cannot observe). A table already auto-routed by a sibling drop, or already sticky on CC, was forwarding the key to Cloud Control all along, which accepts it once AWS's registry carries it. It is now a `silentDrop` entry on both types -- neither SDK provider writes it (`not yet implemented by cdkd`) -- so the #614 auto-route applies: a template carrying it moves that resource onto Cloud Control, which forwards the full property map. Neither DynamoDB type is in `STICKY_CC_MIGRATION_EXEMPT`, so an existing SDK-provisioned table whose template gains the property moves to Cloud Control and pins `provisionedBy: 'cc-api'` ONE-WAY. The snapshot does not mark it create-only, and its capture is top-level-only, so `createOnlyDrops` and the record narrowing are unchanged; a nested create-only path is invisible to that capture, which no refresh fixes.
