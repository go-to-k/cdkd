- **`cdkd local invoke-agentcore --sigv4` now NAMES the `--role-arn` exclusion when it refuses, instead of telling you to set a variable that is already set (issue [#3250](https://github.com/go-to-k/cdkd/issues/3250))** -- `src/cli/commands/local-invoke-agentcore.ts`, `src/utils/caller-credentials.ts` (`envCredentialsAreAssumedRole`), `tests/unit/cli/local-invoke-agentcore-sigv4-identity.test.ts`, `tests/unit/cli/local-container-caller-identity.test.ts`. The signature on `/invocations` is what the emulated agent sees as its INVOKER, so cdkd signs as the CALLER and deliberately never as a `--role-arn` assumed role (issue [#3130](https://github.com/go-to-k/cdkd/issues/3130)). On an SSO / instance-role / container-role caller there is nothing to put back, and the old refusal read `no AWS credentials available... Set AWS_ACCESS_KEY_ID + AWS_SECRET_ACCESS_KEY` while `AWS_ACCESS_KEY_ID` was visibly set -- cdkd had set it, to the role -- so it described cdkd as broken rather than as refusing. It now says which value it declines and why; the remedies are ORDERED per branch (this branch fires only for a caller with no static pair by design, so `--profile` / `--assume-role` lead) and `--profile` drops out when one was already passed. Rationale on `sigv4NoCredentialsRefusal`'s doc comment. Same PR, no behavior delta: `AWS_CREDENTIAL_ENV_KEYS` is now IMPORTED by all three `forwardAwsEnv` bodies rather than documenting a guarantee nothing consumed (item 4); the `cdkd local` credential fence gained the INI credentials-file channel that neither fence could see -- a lowercase `aws_access_key_id` written into a bind-mounted host file (item 7) -- plus comment stripping, a non-empty-reason requirement, a statement-scoped annotation walk in place of two fixed line windows FOUR sites sat exactly on (items 2 and 6), and one shared scope module so the two fences cannot disagree about which files are workload-facing.
