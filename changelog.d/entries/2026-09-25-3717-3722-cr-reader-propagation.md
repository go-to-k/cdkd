- **Two readers of a value that moves in a deploy are updated (issues [#3717](https://github.com/go-to-k/cdkd/issues/3717), [#3722](https://github.com/go-to-k/cdkd/issues/3722))** -- `DiffCalculator` (`extractGetAttRefs` now records `Ref` reads; `promoteInPlaceAttributeDependents` gains the custom-resource `Ref` case and a fresh-parameter arm), `DeployEngine.freshNoEchoParameters`, `carryFreshNoEchoMark` in `src/deployment/secret-redaction.ts` and its call in the resolver's inherited-parameter recording, unit tests in `tests/unit/deployment/` and `tests/unit/analyzer/`, and a new stack plus phase 6 in the `custom-resource-noecho-nested` fixture. A resource reading a custom resource with `Ref` kept the old physical id when the handler answered an Update with a new `PhysicalResourceId`, because the diff promoted `Ref` readers only for a predicted replacement; `Ref` readers of an updated custom resource are now promoted, and the deploy skips each whose id did not move. A resource in a nested stack that reads a `NoEcho` value through a stack parameter kept the old value when the parent's custom resource returned a new one: the child's diff compares the redacted parameter, `***` against `***`. The child engine now promotes readers of a parameter carrying a `NoEcho` value supplied in the same deploy, and that value stays fresh in the reader's own redaction bag, so the no-change skip does not absorb it.
