- **`cdkd import` now redacts the `observedProperties` baseline it captures (issue [#2828](https://github.com/go-to-k/cdkd/issues/2828))** -- `src/cli/commands/import.ts`, three unit suites, one integ fixture. **Bug:** `captureObservedForImportedResources` assigned the provider's `readCurrentState` result onto the record VERBATIM -- the DECRYPTED value for a `{{resolve:secretsmanager:...}}` property -- at both call sites (the root walk and the recursive `--migrate-from-cloudformation` child walk). The gap issue [#1926](https://github.com/go-to-k/cdkd/issues/1926) closed for `cdkd state refresh-observed`. **Fix:** route the captured bag through `redactSecretsForState` with an EMPTY secrets map, positioned against the record's own redacted `properties` under `STATE_SOURCED_READBACK_RULES`; and, that redaction being POSITION-based, refuse a baseline outright for any resource whose resolve THREW or LOST a `{{resolve:` opener the raw template spelled. **PLAINTEXT CAN STILL BE PERSISTED BY THIS CAPTURE**, and `state.json` must still be treated as sensitive -- separately, `cdkd import` also writes `ResourceState.attributes` from a live AWS read through no redactor at all ([#2847](https://github.com/go-to-k/cdkd/issues/2847)). Known ways: a reference the opener comparison cannot see ([#2850](https://github.com/go-to-k/cdkd/issues/2850)); a parameter bound to a placeholder `Default` while the deployed value was the reference ([#2854](https://github.com/go-to-k/cdkd/issues/2854)); a position the redaction walk cannot pair -- reshaped containers, identity keys AWS normalised, readback keys the source lacks, arrays anchors cannot corroborate ([#2852](https://github.com/go-to-k/cdkd/issues/2852), carrying the probe table); a secret with no counterpart in the source ([#2868](https://github.com/go-to-k/cdkd/issues/2868)). Not proven exhaustive: the only evidence is a post-hoc comparison of the persisted bag against the readback, so whatever it cannot see is trusted.
