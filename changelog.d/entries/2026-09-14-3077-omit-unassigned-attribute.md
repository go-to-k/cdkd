- **An attribute a provider could not read back is OMITTED from state instead of recorded as `''` (issue [#3077](https://github.com/go-to-k/cdkd/issues/3077))** -- new leaf `src/provisioning/attribute-map.ts` (`definedAttributes` / `stringifyIfAssigned`), applied at every `?? ''` / `|| ''` reachable from a provider's `attributes` value (96 sites in 18 files): `ec2-provider.ts` (Instance create + update via a state-gated `describedInstanceAttributes`; VPC create), `rds` / `docdb` / `neptune` (cluster + instance, create + update), `ecr`, `elasticache`, `codecommit-repository`, `servicediscovery`, `appsync`, `cloudfront-distribution`, `eventbridge-bus`, `s3-vectors`, `rds-dbproxy(-endpoint)`, four `agentcore-*` providers; `docs/cli-deploy.md`, `docs/provider-development.md`. Under `--no-wait` a `pending` EC2 instance recorded `PublicIp` / `PublicDnsName` as `''` and a `creating` RDS-family `DBInstance` recorded `Endpoint.*` as `''`; state persisted it, the Outputs pass exported it, the resolver served it forever. Absent, an EC2 public address is re-described on the next resolution (the real value once running; while pending the instance id with a warning, nothing cached -- [#3096](https://github.com/go-to-k/cdkd/issues/3096)), and an RDS `Endpoint.*` resolves to the identifier with a warning until the next `update()` ([#1852](https://github.com/go-to-k/cdkd/issues/1852)). An empty string AWS itself reports is KEPT: a running instance with no public address records `''` for both (CloudFormation's answer), so a private-subnet reference does not degrade to the instance id. `Endpoint.Port` is never `'undefined'`. An AppSync `GraphQLUrl` or a CodeCommit `CloneUrl*` / `Arn` missing from the create response now hits the resolver's `*Url` / `*Arn` refusal instead of serving `''`. `tests/unit/provisioning/attribute-map.test.ts` fences the shape; the two `AWS::EC2::SecurityGroup` `VpcId` sites are allow-listed ([#3097](https://github.com/go-to-k/cdkd/issues/3097)).
