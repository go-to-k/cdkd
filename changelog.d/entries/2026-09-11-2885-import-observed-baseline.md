- **`cdkd import`'s observed-baseline capture now FAILS CLOSED at a position it cannot certify (issue [#2885](https://github.com/go-to-k/cdkd/issues/2885))** -- `src/cli/commands/import.ts`, `secret-redaction.ts` (doc only), `tests/unit/cli/import-observed-baseline-refusal-matrix.test.ts`, `docs/{import,cli-drift}.md`, `.claude/rules/layout-deployment-secrets.md`. **Bug:** the capture passed `STATE_SOURCED_READBACK_RULES`, so the residue (#2852) left behind was live: where the readback and the record could not be paired at a reference-bearing position, the DECRYPTED value was persisted into `observedProperties` in `state.json` -- the GHSA-p5qg-v9gv-hc7w direction, on ordinary shapes (a reshaped container, an added wrapper level, a scalar returned as a container, an identity key AWS normalised, a list AWS reordered or whose length it changed, two lookalike elements, a bare-token element with no literal frame). **Fix:** it passes `STATE_SOURCED_BASELINE_RULES`, the constant a caller DECLARES when its bag becomes a drift baseline and nothing else -- which this writer's does. `cdkd drift --accept` keeps the other constant because for a record with NO baseline it writes into `properties`, where a mask blocks `cdkd export` and the rollback replay. **Cost, on a population the pre-existing skip does not cover:** a resource the classifier ADMITS whose readback cannot be paired keeps its record and its baseline with `***` at the uncertified leaves, so it reports as drifted on every run and `--accept` refuses it until a deploy CREATES or UPDATES that resource (the auto-refresh fills only a MISSING one). Over-masking is the deliberate direction; a resource with no dynamic reference cannot be masked at all. **Still open, measured rather than inferred:** a dropped or traded reference ((#2850)), a parameter bound to a placeholder `Default` ((#2854)), and a secret with no counterpart in the source ((#2868)) -- the last of which (#2852)'s own note listed as closed and is not.
