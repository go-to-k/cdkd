- **`cdkd drift --revert`'s masked-leaf preservation no longer pairs a list element whose identity field is a whole `{{resolve:...}}` token with the live element that literally echoes the token text (issue [#2919](https://github.com/go-to-k/cdkd/issues/2919))** -- `src/cli/commands/drift.ts` (`pairedLiveItems`), unit tests. The identity-arm refusal was keyed on the caller's corroboration wildcard, so the MASK walk (wildcard: the mask alone) still paired a token-named element by string equality and donated the echo's live leaves into the masked slots -- an uncorroborated guess. The identity refusal is now `isTokenOrMaskLeaf` for both walks; the mask walk's residual there is its safe drop (the resource is refused with the position named), and the token walk's answer is unchanged.
